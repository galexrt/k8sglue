{{ pillar['headers']['salt']['file'] }}

roster: flat
roster_file: /etc/salt/roster
roster_defaults:
  sudo: true
  priv: /etc/salt/ssh/id_rsa
  thin_dir: /var/tmp/.salt-ssh
rosters:
  - /etc/salt/roster.d
